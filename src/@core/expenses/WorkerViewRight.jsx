import UserViewOverview from './WorkerViewOverview'
const UserViewRight = ({ selectedWorker, invoiceData }) => {
  return (
    <UserViewOverview selectedWorker={selectedWorker} invoiceData={invoiceData} />
  )
}

export default UserViewRight
