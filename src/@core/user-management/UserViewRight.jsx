import UserViewOverview from './UserViewOverview'
const UserViewRight = ({ selectedUser, invoiceData }) => {
  return (
    <UserViewOverview selectedUser={selectedUser} invoiceData={invoiceData} />
  )
}

export default UserViewRight
